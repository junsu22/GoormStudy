# 순열/ 조합을 활용한 예제

# 예제 1 52 장의 포커 카드에서 5장의 카드 를 고르는 경우의 수는?

print(factorial(52) / factorial(5) / factorial(47))

# 2598960


# 예제 2 500개의 전구로부터 5개의 백열전구를 택하는 방법의 개수는?

print(binomail(500, 5))

# 255244687600


# 동전을 10,000 번 던졌을때 , 앞면이 나올 확률
import random

coin = ["앞", "뒤"]
count = 1
loopCount = 100000

for i in range(loopCount):
    temp = random.choice(coin)
    if temp == "앞":
        count += 1
print("동전에서 앞면이 나올 확률: ", count / loopCount)

# 동전에서 앞면이 나올 확률:  6203/12500

"""
예제4 3대의 기계 A, B, C 가 각각 이 공장의 생산품 전체의 50%, 30%, 20% 를 생산한다.
그리고 이들 기계가 불량품을 생산할 비율은 각각 4%, 3%, 2%이다. 한 제품을 임으로 선택할 때
그 제품이 불량인 확률을 구하여라. 또한 불량품이 기계 C에 의하여 생산될 확률을 구하여라.
"""
# 생산률
# P(A) = 0.5, P(B) = 0.3, P(C) = 0.2
# 불량률
# P(X|A) = 0.04, P(X|B) = 0.03, P(X|C) = 0.02

"""
임의로 뽑은 제품이 불량일 확률 P(X)
불량품일 때 기계 C에서 생산되었을 확률 P(C|X)

"""
# 생산비율
PA = QQ(1 / 2)  # 0.5
PB = QQ(3 / 10)  # 0.3
PC = QQ(1 / 5)  # 0.2

# 불량률 (조건부 확률)
PX_A = QQ(4 / 100)  # P(X|A)
PX_B = QQ(3 / 100)  # P(X|B)
PX_C = QQ(2 / 100)  # P(X|C)

# 전체 불량확률 P(X)
PX = PA * PX_A + PB * PX_B + PC * PX_C
print("P(X) = ", PX, "approx = ", N(PX))

# 베이즈 정리 P (C|X)
PC_X = (PC * PX_C) / PX
print("P(C|X) = ", PC_X, "approx = ", N(PC_X))

"""
P(X) =  33/1000 approx =  0.0330000000000000    # 전체 불량 확률
P(C|X) =  4/33 approx =  0.121212121212121  # 불량이 C에서 생산되었을 확률

"""
"""

# 예제1 확률변수 X 의 확률분포가 다음과 같을 때, 기댓값과 분산, 표준편차를 구하여라.

    x   |  0 |  1   |   2   |   3    | 합계
  f(x) |0.010| 0.840| 0.145 | 0.005 | 1 
  
  
  """
x = [0, 1, 2, 3]
f = [QQ(0.010), QQ(0.840), QQ(0.145), QQ(0.005)]

# Expectation
EX = sum(x[i] * f[i] for i in range(len(x)))
print("E[X] = ", EX)
print("E[X] = approx = ", N(EX))

# Variance
EX2 = sum((x[i] ^ 2) * f[i] for i in range(len(x)))
VarX = EX2 - EX ^ 2
print("Var(X) = ", VarX)
print("Var(X) approx = ", N(VarX))

# Standard deviation
StdX = sqrt(VarX)
print("Std(X) = ", StdX)
print("Std(x) approx = ", N(StdX))
"""
기댓값: 확률변수 X가 평균적으로 어느 정도의 값을 가지는지 나타낸다.
E[X] = 229/200
E[X] approx = 1.145   # 근사값

분산: 값들이 기댓값(평균)을 기준으로 얼마나 퍼져 있는지를 나타낸다.
Var(X) = 6159/40000
Var(X) approx = 0.153975

표준편차: 분산의 제곱근으로, 평균에서의 실제적인 변동 폭을 의미한다.
Std(X) = 1/200*sqrt(6159)
Std(X) approx = 0.392396


"""

"""
확률 밀도 함수
예제 2 확률변수 X 의 확률 밀도 함수가 f(x) = 3x^2 , 0 < x < 1 일때,
X 와 Y = 4X + 2의 분산을 구하여라.

"""
var("t")
f = 3 * t ^ 2
Ex = integral(t * f, t, 0, 1)
Ex2 = integral((t ^ 2) * f, t, 0, 1)
print(Ex)
print(Ex2)
print(Ex2 - Ex ^ 2)  # X의 분산
Ey = integral((4 * t + 2) * f, t, 0, 1)
Ey2 = integral((4 * t + 2) ^ 2 * f, t, 0, 1)
print(Ey)
print(Ey2)
print(Ey2 - Ey ^ 2)  # Y = 4X + 2 의 분산

"""
3/4
3/5
3/80
5
128/5
3/5
"""

"""
베르누이 분포
1회 시행의 결과가 성공 또는 실패 두 가지중 하나로만 나오는 실험.
ex. 동전던지기 성공을 1, 실패를 0으로 결과를 나타낸다.

예제 1 확률변수 X 를 주사위 한개를 던져 나온 결과로 가정,
1이 나오면 X = 1 , 그외 숫자는 X= 0
X의 확률분포 P( X = x )를 구하라.
"""

p = [(1 / 6) ^ t * (5 / 6) ^ (1 - t) for t in range(0, 2)]
print(p)

# [5/6, 1/6]
# 베르누이 시행에서 X 의 확률분포


"""
이항분포와 포아송분포를 활용한 예시를 만들고,
X를 50,000회 시뮬레이션하여 P(X=3)을 추정한 뒤
이항분포 이론값 / 포아송 근사값과 비교한다.

던전에서 몬스터를 n=1200마리 잡는다.
각 몬스터가 레어 아이템을 드롭할 확률은 p=0.002 (서로 독립).
X = 드롭된 레어 아이템 개수

1) 이항분포로 P(X=3) 계산
2) 포아송 근사(lam = n*p)로 P(X=3) 계산
3) X를 5만 번 시뮬레이션해서 P(X=3) 추정 + 비교

"""

# Sage 난수 시드 고정
set_random_seed(42)

import random
import math

# 파이썬에서
# import random
# random.seed(42)


# dungeon drop setting
n = 1200  # 몬스터 수(시행 횟수)
p = 0.002  # 몬스터 1마리당 레어 아이템 드롭 확률
lam = n * p  # 평균적으로 던전 한 판에서 레어템이 몇 개 나오는지(포아송 근사)
k = 3  # 관심 사건: X가 3일 확률(예시로 선택한 값)
# 꼭 3개가 아닌 0,1,2,4,...도 나올 수 있음.
# 많은 판 중에 "3개를 드롭한 판"이 얼마나 자주 나오나? 를 보는 중
Nsim = 50000  # 시뮬레이션 반복 횟수

# 1) 이항분포 이론값 (정확)
# X ~ Binomial(n, p)
# k : 보고싶은 값 / p : 한번의 성공확률
# 몬스터를 n번 잡았을 때, 레어템이 정확히 k번 나올 확률
P_binom = binomial(n, k) * (p ^ k) * ((1 - p) ^ (n - k))

# 2) 포아송 근사값
# 정확히 따지기보단, 평균적으로 몇 번 일어나는지만 보고 확률 계산
P_pois = exp(-lam) * (lam ^ k) / factorial(k)


# 3) 시뮬레이션으로 P(X=3) 추정
# 웹 SageMathCell에서 몬스터를 1200마리씩 직접 굴리면 너무 무거워서 실행이 안 됨
# 포아송 분포에서 X를 직접 샘플링(한 판 결과를 하나 뽑아본다)하는 방식으로
# X를 5만 번 생성하여 확률을 추정한다.
def poisson_sample(lam):
    L = math.exp(-lam)
    s = 1.0
    x = 0
    while s > L:
        s *= random.random()
        x += 1
    return x - 1


# 레어템 드롭 0번
count_eqk = 0
for _ in range(Nsim):
    X = poisson_sample(lam)
    if X == k:  # 원하던 결과인가 (3개가 드롭된 판)
        count_eqk += 1  # 이번 판 3개 나왔으면 카운트 + 1
# 던전 Nsim(50000)번 반복...
P_sim = count_eqk / Nsim

# 결과 출력
print("=== 레어 아이템 드롭 시뮬레이션 ===")
print(
    f"n(몬스터 수)={n}, "
    f"p(드롭 확률)={p}, "
    f"lam=n*p(평균 드롭 수)={lam}, "
    f"k(관심값)={k}, "
    f"시뮬 횟수={Nsim}\n"
)

print("[이항분포 이론값]  P(X=3) =", N(P_binom))
print("[포아송 근사값]    P(X=3) =", N(P_pois))
print("[시뮬레이션 추정]  P(X=3) =", P_sim)

print("\n[오차 비교]")
print("시뮬레이션 - 이항분포 절대오차 =", abs(N(P_sim) - N(P_binom)))
print("포아송근사 - 이항분포 절대오차 =", abs(N(P_pois) - N(P_binom)))

"""
=== 레어 아이템 드롭 시뮬레이션 ===
n(몬스터 수)=1200, p(드롭 확률)=0.00200000000000000, lam=n*p(평균 드롭 수)=2.40000000000000, k(관심값)=3, 시뮬 횟수=50000

[이항분포 이론값]  P(X=3) = 0.209244429379062
[포아송 근사값]    P(X=3) = 0.209014164378806
[시뮬레이션 추정]  P(X=3) = 10251/50000

[오차 비교]
시뮬레이션 - 이항분포 절대오차 = 0.00422442937906231
포아송근사 - 이항분포 절대오차 = 0.000230265000255914


포아송의 정의 : 
포아송 분포란,
시행 횟수가 많고 개별 사건의 발생 확률이 매우 작은 상황에서,
평균 발생 횟수를 기준으로
사건의 발생 횟수를 근사적으로 나타내는 분포이다.

문제에서는 몬스터 수가 많고 드롭 확률이 매우 작아
이항분포를 포아송 분포로 근사할 수 있었다.

"""


# 균등분포 예제
"""
예제 6. 확률변수 X가 U (0,10)를 따른다고 하자. 다음 확률을 구하여라.
(1) P(X<3)
(2) P(X>6)
(3) P(3<X<8)
"""
print(integral(1 / 10, x, 0, 3))
print(integral(1 / 10, x, 6, 10))
print(integral(1 / 10, x, 3, 8))

"""
3/10    # 0.3   P(X<3)
2/5     # 0.4   P(X>6)
1/2     # 0.5   P(3<X<8)
"""
