# Groq + LLaMA 연결 (모델 비교해보기, 멀티턴)
# pip install groq

# API 발급 필요
# https://console.groq.com/keys


from groq import Groq

client = Groq(api_key="")


# 대화 기록을 리스트로 관리 (더 추가 하면 됨)
messages = [
    {
        "role": "system",
        "content": "너는 Python 튜터야. 초보자도 이해할 수 있게 쉽게 설명해줘. 답변은 한국어로 해줘.",
    }
]

while True:
    # 1. 사용자 입력 받기 
    # 사용자 입력   / Input 으로 부터 입력 받아서 답변  / 대화종료 방법 : 종료
    user_input = input("나: ")
    if user_input == "종료":
        break

    # 2.대화 기록에 추가
    # 사용자 메시지 추가 , (위에서 한 대화기록을 입력받아서 리스트를 추가해 나감.)
    messages.append({"role": "user", "content": user_input})
    # 3. API요청 (대화기록전체를 넘김)
    # AI 답변 받기
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile", messages=messages  # 어떤 모델을 사용할 것 인가. 지금까지 쌓인 대화를 넘김
    )
    # 4. 답변 꺼내기
    ai_reply = response.choices[0].message.content
    print(f"AI: {ai_reply}\n")
    # 5. 다음 대화를 위해 답변도 함께 대화 기록에 추가
    # AI 답변도 기록에 추가
    messages.append({"role": "assistant", "content": ai_reply})


'''
나: 안녕
AI: 안녕하세요! 파이썬 공부에 어떤 도움이 필요하세요? 
파이썬의 기본부터 프로젝트에 대한 도움까지, 저희가 도와드리겠습니다. 오늘은 어떤 주제로 학습할 예정인가요?

나: 딕셔너리에 대해 알고싶어 
AI: 파이썬의 딕셔너리(dictionary)는 데이터를 저장하고 관리하기 위한 매우 유용한 자료구조입니다. 
딕셔너리는 키(key)와 값(value)의 짝으로 구성되어 있으며, 키를 사용하여 값을 조회할 수 있습니다.

### 딕셔너리 생성

딕셔너리를 생성하는 방법은 여러 가지가 있습니다.

```python
# 방법 1: 빈 딕셔너리 생성
empty_dict = {}

# 방법 2: 초기값을 가진 딕셔너리 생성
person = {"name": "홍길동", "age": 30}

# 방법 3: dict() 함수를 사용한 딕셔너리 생성
person = dict(name="홍길동", age=30)
```

### 딕셔너리 요소 접근 및 수정

딕셔너리의 요소를 접근하거나 수정하려면 키를 사용합니다.

```python
person = {"name": "홍길동", "age": 30}

# 키를 사용하여 값을 조회
print(person["name"])  # 홍길동

# 키를 사용하여 값을 수정
person["age"] = 31
print(person["age"])  # 31
```

### 딕셔너리에서 키 및 값 반복

딕셔너리에 있는 모든 키와 값을 반복하여 출력할 수 있습니다.

```python
person = {"name": "홍길동", "age": 30}

# 키만 반복
for key in person.keys():
    print(key)

# 값만 반복
for value in person.values():
    print(value)

# 키와 값을 함께 반복
for key, value in person.items():
    print(f"{key}: {value}")
```

### 딕셔너리 요소 추가 및 삭제

딕셔너리에 새로운 키-값 쌍을 추가하거나 기존의 키-값 쌍을 삭제할 수 있습니다.

```python
person = {"name": "홍길동", "age": 30}

# 새로운 키-값 쌍 추가
person["city"] = "서울"
print(person)  # {"name": "홍길동", "age": 30, "city": "서울"}

# 기존 키-값 쌍 삭제
del person["age"]
print(person)  # {"name": "홍길동", "city": "서울"}
```

파이썬의 딕셔너리는 다양한 상황에서 데이터를 효율적으로 관리할 수 있는 강력한 도구입니다. 
위의 예제를 통해 기본적인 사용법을 익힌 후, 실제 프로젝트에서 다양하게 응용해 보세요!       

나: 파이썬의 메모리에 대해 쉽게 설명해줘
AI: 파이썬의 메모리 관리는 개발자가 직접 관리할 필요가 없습니다. 
파이썬에는 가비지 컬렉터(Garbage Collector)라는 시스템이 내장되어 있기 때문입니다.
가비지 컬렉터는 파이썬이 동적으로 메모리를 관리하는 핵심 메커니즘입니다.

### 메모리 할당

파이썬에서 변수를 선언하면, 파이썬의 메모리 관리자가 자동으로 메모리를 할당합니다. 
예를 들어, 다음과 같은 코드를 실행하면:

```python
a = 10
```

파이썬은 내부적으로 다음과 같은 프로세스를 거칩니다.

1. **객체 생성**: 파이썬은 `10`을 가리키는 객체를 생성합니다. 
이 객체에는 데이터(`10`)와 메타데이터(객체의 유형, 크기 등)가 저장되어 있습니다.
2. **메모리 할당**: 파이썬은 이 객체를 위한 메모리를 할당합니다. 
메모리 할당은 파이썬의 메모리 관리자에 의해 자동으로 처리됩니다.
3. **参照 생성**: 변수 `a`는 생성된 객체를 가리키는 참조(참조자, 포인터)를 받습니다.

### 참조 카운트

파이썬의 가비지 컬렉터는 참조 카운트(Reference Counting)라는 기법을 사용하여 
객체의 生命周期을 관리합니다. 참조 카운트는 객체를 가리키는 참조의 개수를 계산하는 기법입니다.

객체가 생성되면, 참조 카운트는 1에서 시작합니다. 
객체를 가리키는 참조가 새로 추가되면, 참조 카운트가 증가합니다. 예를 들어, 다음과 같은 코드를 실행하면:

```python
a = 10
b = a
```

객체 `10`을 가리키는 참조가 2개가 되므로, 참조 카운트는 2가 됩니다.

반면, 객체를 가리키는 참조가 삭제되면, 참조 카운트가 감소합니다. 
예를 들어, 다음과 같은 코드를 실행하면:

```python
a = 10
b = a
del a
```

객체 `10`을 가리키는 참조가 1개만 남아 있으므로, 참조 카운트는 1이 됩니다.

### 가비지 컬렉션

객체의 참조 카운트가 0이 되면, 객체는 더 이상 필요하지 않으며, 가비지 컬렉터에 의해 메모리에서 삭제됩니다. 
예를 들어, 다음과 같은 코드를 실행하면:

```python
a = 10
b = a
del a
del b
```

객체 `10`을 가리키는 참조가 모두 삭제되므로, 참조 카운트는 0이 됩니다. 
가비지 컬렉터는 이 객체를 메모리에서 삭제하여, 메모리를 회수합니다.

### 마무리

파이썬의 가비지 컬렉터는 개발자가 메모리를 직접 관리할 필요가 없게 해줍니다. 
하지만, 가비지 컬렉터의 동작을 이해하여, 메모리 사용을 최적화하고, 성능을 개선할 수 있습니다. 
특히, 대규 모 데이터를 처리하거나, 성능에 민감한 애플리케이션을 개발하는 경우, 
메모리 사용을 최적화하여 애플리케이션의 성능을 향상시킬 수 있습니다.

나: 더 쉬운 설명   
AI: 파이썬의 메모리는 컴퓨터의 저장 공간입니다. 파이썬에서 변수를 만들면, 메모리에 저장됩니다.

1. **메모리 할당**: 파이썬은 변수를 만들 때, 메모리에 공간을 할당합니다.
2. **참조**: 변수는 메모리에 저장된 데이터를 가리킵니다.
3. **삭제**: 변수를 삭제하면, 메모리에서 데이터가 삭제됩니다.

파이썬에는 **가비지 컬렉터**라는 시스템이 있습니다. 
가비지 컬렉터는 메모리에서 더 이상 필요하지 않은 데이터를 자동으로 삭제합니다.

예를 들어,

* `a = 10`을 하면, 메모리에 `10`이 저장되고, `a`는 `10`을 가리킵니다.
* `b = a`를 하면, `b`도 `10`을 가리킵니다.
* `del a`를 하면, `a`는 더 이상 `10`을 가리키지 않습니다. 
하지만, `b`는 아직 `10`을 가리키고 있으므로, 데이터는 삭제되지 않습니다.
* `del b`를 하면, 더 이상 `10`을 가리키는 변수가 없으므로, 메모리에서 `10`이 삭제됩니다.

이렇게 파이썬의 메모리는 자동으로 관리되므로, 개발자가 메모리를 직접 관리할 필요는 없습니다.

나: 종료   
PS C:\Users\pc\Desktop\test\0217> 

'''